# Fingerprint Reader

This one just worked: https://github.com/uunicorn/python-validity

But breaks after sleep. Just enable the service `sudo systemctl enable python3-validity-restart-after-resume.service`
